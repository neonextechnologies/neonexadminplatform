# Cursor Prompts (Copy/Paste)

ไฟล์นี้เป็น “ข้อความตั้งต้น” สำหรับใช้กับ Cursor/AI เพื่อกัน drift ให้ทำงานตามแผนรีบิวด์แบบเคร่งครัด

## วิธีใช้ (สั้น ๆ)
- ใช้ **Cursor Standard Prompt** ตอนเริ่มงานก้อนใหม่/เริ่ม session/หรือเมื่อเริ่มหลุด scope
- ใช้ **Cursor Mini Prompt** ตอนต้อง “รีเซ็ตกรอบเร็ว ๆ” ระหว่างงาน โดยแทน `Phase X` เป็น Phase ที่กำลังทำ

---

## 🤖 Cursor Standard Prompt (Copy/Paste)

1) ทำตาม `PROJECT_REBUILD_PLAN_BOOTSTRAP_JQUERY.md` แบบเคร่งครัด และยึด “Layer gates” เป็นกติกาหลัก
2) ตอนนี้ทำเฉพาะ Layer A (Phase 0–7) เท่านั้น จนกว่า Gate A→B จะผ่าน
3) ห้ามทำ/ห้ามพึ่ง component library (`<x-limitless::...>`) และห้ามผูก DataTables เป็น baseline ใน Layer A
4) UI ใน Layer A ให้ใช้ plain Bootstrap markup (SSR Blade) + jQuery action router (`data-action`) เท่านั้น
5) ห้ามใช้ npm build / bundler / SPA frameworks (Vue/React/Livewire/Inertia)
6) ทุก route admin ต้องมี `auth` + `tenant.selected` (หลัง Phase 5) และต้อง tenant-safe เสมอ
7) RBAC ต้องเป็น registry-first: permission ต้อง register ผ่าน service/registry กลาง (ไม่กระจัดกระจายใน controller)
8) CRUD สำคัญต้อง audit-first: create/update/delete ต้องเรียก `audit()->record(...)`
9) ถ้าต้องตัดสินใจแบบกำกวม ให้เลือกทางที่ “เรียบที่สุด” และไม่เพิ่ม UI/feature นอกสcope ของ Phase นั้น
10) ถ้าจะสร้างไฟล์/โฟลเดอร์ใหม่ ให้ทำตามโครง Kernel/Modules/Theme adapter ที่เอกสารกำหนด
11) เพิ่มตัวอย่าง seed/fixtures/tests เท่าที่จำเป็นเพื่อพิสูจน์ว่า tenant/RBAC/audit ทำงานจริง
12) ทุก output ต้องมี “Exit criteria” ของ Phase นั้นผ่านก่อน แล้วค่อยไปขั้นถัดไป
13) ห้ามสร้าง feature/ไฟล์นอก Phase ปัจจุบัน แม้จะดู “เหมาะสม” ก็ตาม
---

### Cursor Mini Prompt (3–5 บรรทัด ใช้แปะบ่อย ๆ)

1) ทำเฉพาะ Phase X (Layer A) ตามเอกสารนี้ ห้ามออกนอก scope
2) ห้าม `<x-limitless::...>` และห้าม DataTables baseline (จนกว่าจะถึง Phase 8.0 / Layer B)
3) UI ใช้ plain Bootstrap (SSR Blade) + jQuery action router (`data-action`) เท่านั้น
4) ต้อง tenant-safe + registry-first RBAC + audit-first ตามกติกา
5) Output ต้องบอก Exit criteria ของ Phase นี้ว่าผ่านอะไรแล้วบ้าง
6) ห้าม component library
7) ต้อง registry-first + audit-first